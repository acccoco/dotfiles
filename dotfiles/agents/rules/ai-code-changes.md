# 对 AI 修改代码的约束

请将代码改动加工成便于人工 Review 的形式：

给出设计方案、修改代码时，应区分设计理念、设计不变量和当前实现细节。主动说明方案保持或改变了哪些重要不变量，以及相应证据和风险，但不要求使用固定的不变量登记格式。

- 每个提交只能包含一个可验证的行为变化。
- 每个提交必须可独立编译和回滚。
- 不以 300 行作为唯一拆分标准；优先按语义和风险拆分。
- 机械性代码应单独提交并明确标注。
- 无法证明的结论必须标明是推断，不得宣称已经验证。
